<Window x:Class="RunJargon.App.Windows.TranslationResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        Topmost="True"
        Background="Transparent">
    <Window.Resources>
        <Style x:Key="MinimalThumbStyle"
               TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="#94A3B8"
                                CornerRadius="999" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MinimalScrollBarStyle"
               TargetType="ScrollBar">
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="Opacity"
                    Value="0.28" />
            <Setter Property="Width"
                    Value="6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track"
                                   IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand"
                                                  Opacity="0"
                                                  Focusable="False" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource MinimalThumbStyle}" />
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand"
                                                  Opacity="0"
                                                  Focusable="False" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation"
                         Value="Horizontal">
                    <Setter Property="Height"
                            Value="6" />
                </Trigger>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Opacity"
                            Value="0.72" />
                </Trigger>
                <Trigger Property="IsKeyboardFocusWithin"
                         Value="True">
                    <Setter Property="Opacity"
                            Value="0.72" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MinimalCloseButtonStyle"
               TargetType="Button">
            <Setter Property="Background"
                    Value="#EEF2F7" />
            <Setter Property="BorderBrush"
                    Value="#D6DEE8" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="Foreground"
                    Value="#0F172A" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="999">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="Chrome"
                                        Property="Background"
                                        Value="#E2E8F0" />
                                <Setter TargetName="Chrome"
                                        Property="BorderBrush"
                                        Value="#CBD5E1" />
                            </Trigger>
                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter TargetName="Chrome"
                                        Property="Background"
                                        Value="#D8E1EC" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="RootBorder"
            Padding="10"
            Background="#F3F8FAFC"
            BorderBrush="#D7DEE8"
            BorderThickness="1"
            CornerRadius="16"
            MouseLeftButtonDown="RootBorder_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28"
                              ShadowDepth="10"
                              Opacity="0.18"
                              Color="#0F172A" />
        </Border.Effect>

        <Grid>
            <TextBox x:Name="TranslationTextBox"
                     Margin="0,0,30,0"
                     Background="Transparent"
                     BorderThickness="0"
                     IsReadOnly="True"
                     IsReadOnlyCaretVisible="False"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     SpellCheck.IsEnabled="False"
                     Foreground="#111827"
                     SelectionBrush="#BFDBFE"
                     FontSize="13"
                     FontWeight="Normal"
                     CaretBrush="Transparent"
                     Cursor="IBeam"
                     Text="Перевод появится здесь.">
                <TextBox.Resources>
                    <Style TargetType="ScrollBar"
                           BasedOn="{StaticResource MinimalScrollBarStyle}" />
                </TextBox.Resources>
            </TextBox>

            <Button x:Name="CloseButton"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Width="22"
                    Height="22"
                    Padding="0"
                    Click="CloseButton_Click"
                    Style="{StaticResource MinimalCloseButtonStyle}"
                    FontSize="11"
                    FontWeight="Bold"
                    Content="×" />
        </Grid>
    </Border>
</Window>
